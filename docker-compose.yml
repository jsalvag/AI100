services:
  ai-lab:
    build: .
    ports:
      - "4321:4321"
    environment:
      AI_DATA_DIR: /data
      HOST: 0.0.0.0
      PORT: 4321
    volumes:
      - ai_lab_data:/data

volumes:
  ai_lab_data:
