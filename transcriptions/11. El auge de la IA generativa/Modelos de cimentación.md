- So now you've seen that self-supervised learning
is a powerful new machine learning technique.
It's like a giant vacuum that sucks up
all the data in the world and gives it an accurate label.
It does this without very much human intervention.
This technique moves beyond data models
and predictive artificial intelligence.
That's why this is the primary technique
that you use to create generative AI systems.
Remember that generative AI systems are much more flexible.
When you ask a generative AI chatbot a question,
it needs to have wide knowledge of many different topics.
It needs to be able to connect birds, cars,
television shows, and a bunch of other data.
In the past, you would have to retrain your model
anytime you added new data.
That's why in 2021, a group at Stanford University
came up with a new way to think about models.
Data models were fine tuned for a specific task.
So instead, they proposed creating foundation models
that could do a wide range of tasks
without retraining on new data.
So think about how this might work
with self-supervised learning.
Imagine that you wanted to vacuum up all the data
from the United States Library of Congress.
You could use unsupervised learning to cluster together
different ideas and topics.
You can almost think of this as a similar stuff cluster.
These could be people, places, and things
that are somehow interconnected.
Imagine that you had an unsupervised cluster of information
about ancient sculptures.
You could then use a form of supervised learning
to label this cluster for your chatbot.
Since they're not human-created labels,
they're called pseudo labels.
These pseudo labels might include the words Greek,
marble, Acropolis, Florence, Michelangelo, and Pheidias.
The machine could potentially create
billions of these pseudo labels.
It could also vacuum up all the data
on something like French fries.
Then the system could create pseudo labels for crinkle cut,
Belgian, steak fries, and even fast food restaurants.
Now this self-supervised learning system
could build its foundation model.
It could have billions of pseudo labels
with trillions of bits of data.
This foundation model gives the AI system
a much wider view of the world.
That's how you can use these GenAI systems
to create a marble sculpture of a French fry.
It has the pseudo labels and the data it needs
to make an accurate image.
That's also why these generative AI chat systems
do something called hallucinate.
This is when the system says something
that sounds like it could be accurate, but it isn't true.
It might say that George Washington
went to the University of Wisconsin,
or that you should eat pebbles to improve your health.
What's happening that the system is creating billions
of these pseudo labels.
After vacuuming up all that data,
it mismatched a connection.
Maybe a professor from the University of Wisconsin
wrote a great book on George Washington.
Then the system made a mismatch.
Now, the key thing to keep in mind is that foundation models
are completely different from data models.
In fact, foundation models can sometimes be used
to make predictions, but data models can't be used
to make generative AI systems.
Foundation models are powerful and flexible,
and data models are streamlined
but trained for just one task.
